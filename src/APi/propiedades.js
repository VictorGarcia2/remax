
/* import axios from "axios";
const instance = axios.create({
    baseURL: './propiedades.js', // Replace with your actual base URL
});

export async function Propiedades() {
    try {
        const {status, data} = await instance.get('/propiedades') // Replace with your actual endpoint
        return {data, status}

    } catch (error) {
      throw error
    }
    
}
const [data, setData] = useState([])
   console.log(data)
  useEffect(() => {
    Propiedades()
      .then((response) => setData(response))
      .catch(error => console.error(error))
  }, [])
 */